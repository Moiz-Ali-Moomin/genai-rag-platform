version: 0.2

phases:
  build:
    commands:
      - echo "Current directory:"
      - pwd
      - echo "Files in current directory:"
      - ls -la
      - echo "Recursive file list:"
      - find . -maxdepth 3
