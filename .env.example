# AWS Configuration
AWS_DEFAULT_REGION=us-east-1
AWS_PROFILE=default

# App Configuration
ENVIRONMENT=development
LOG_LEVEL=INFO

# Bedrock Knowledge Base
BEDROCK_KNOWLEDGE_BASE_ID=xxxxxxxxx

# OpenAI Configuration
OPENAI_MODEL=gpt-4
OPENAI_API_KEY=REDACTED

# OpenSearch Configuration (Optional if not using Bedrock directly)
# OPENSEARCH_ENDPOINT=https://your-opensearch.endpoint.com
# OPENSEARCH_INDEX=rag-index
